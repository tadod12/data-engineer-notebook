guesses = ['vanh', 'lby', 'gb', 'ichbingt', 'john weak', 'a']

guess = guesses.pop()
print(f'Can not invite {guess}')

guess = guesses.pop()
print(f'Can not invite {guess}')

guess = guesses.pop()
print(f'Can not invite {guess}')

guess = guesses.pop()
print(f'Can not invite {guess}')

print(f'Final guesses: {guesses[0]}, {guesses[1]}')
